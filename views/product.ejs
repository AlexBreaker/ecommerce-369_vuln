<!DOCTYPE html>
<html>
<head>
  <title>Product Details</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <div class="container mx-auto p-4">
    <h1 class="text-2xl font-bold mb-4"><%= product.name %></h1>
    <img src="<%= product.imageUrl %>" alt="<%= product.name %>" class="w-64 h-64 object-cover">
    <p class="text-xl">$<%= product.price %></p>
    <p><%= product.description %></p>
    <h2 class="text-xl mt-4">Test Image Fetch (Vulnerable Feature)</h2>
    <form action="/fetch-image" method="POST">
      <input type="text" name="url" placeholder="Enter image URL" class="border p-2 w-full">
      <button type="submit" class="bg-red-500 text-white p-2 rounded">Fetch Image</button>
    </form>
    <a href="/" class="mt-4 inline-block text-blue-500">Back to Products</a>
  </div>
</body>
</html>